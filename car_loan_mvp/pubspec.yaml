name: car_loan_mvp
description: Car Loan Negotiation Assistant Flutter App
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  file_picker: ^6.1.1

flutter:
  uses-material-design: true
